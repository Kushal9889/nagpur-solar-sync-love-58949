# Stripe API Configuration (TEST MODE ONLY)
# Get your test API key from: https://dashboard.stripe.com/test/apikeys
STRIPE_API_KEY=sk_test_your_test_key_here
STRIPE_SIGNING_SECRET=whsec_your_webhook_signing_secret_here

# MongoDB Configuration
MONGO_URL=mongodb://localhost:27017/solar-panda

# Server Configuration
PORT=3000
NODE_ENV=development

# Analytics Configuration
ANALYTICS_ENABLED=true

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace placeholder values with your actual test-mode credentials
# 3. NEVER commit the .env file to version control
# 4. Use Stripe test mode keys (start with sk_test_)
# 5. Get webhook signing secret by running: stripe listen --forward-to localhost:3000/stripe/webhook
